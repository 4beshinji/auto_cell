.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/
.env
# Local development infrastructure (e.g. MQTT broker configs, runtime data).
infra/mosquitto/data/
infra/mosquitto/log/
# App: DO commit uv.lock (reproducible). Intentionally NOT ignored.

# Runtime data directories (event store, audit logs, auth DB, approval DB).
data/

# Raw KG seed archive — canonical content lives extracted in docs/knowledge_graph/
files.zip
